# Weather CLI

A Node.js command-line weather application.

## Features
- Get current weather, today, or full week forecast
- Automatic cache
- Handles city names with accents

## Usage
```bash
node weather.js CITY_NAME --now
node weather.js CITY_NAME --today
node weather.js CITY_NAME
